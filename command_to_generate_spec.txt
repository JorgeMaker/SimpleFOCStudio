pyi-makespec --onefile --noconsole --icon src/gui/resources/studioicon.icns --add-data src/gui/resources/*:src/gui/resources/ simpleFOCStudio.py

NOTE: the current pipeline uses an older version of pyinstaller that does not support a list of icon paths, so the generated spec file must be edited such that the icon is set to a str value:
icon=['src\\gui\\resources\\studioicon.icns'], -> icon='src\\gui\\resources\\studioicon.icns',
